apiVersion: v1
kind: Service
metadata:
  name: {{ include "datakern.fullname" . }}-backend-svc
  labels:
    {{- include "datakern.labels" . | nindent 4 }}
spec:
  ports:
    - port: 8000
      protocol: TCP
      targetPort: http
  selector:
    {{- include "datakern.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: {{ include "datakern.fullname" . }}-backend
